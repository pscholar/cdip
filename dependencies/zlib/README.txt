This folder is meant contain the zlib
Specifically tow folders:
-include that contains the following png header files:
+zlib.h,zconf,pnglibconf.
-lib folder that contains the libz builds:
+zlib.a,......
However, these dependencies can be installed anywhere else on
your PC.
You would have to edit the build file accordingly.
